import VideoCountEventEmitter, { VideoCountEvents } from '../events/VideoCountEventEmitter.js';
declare class VideoCountCommand {
    private emitter;
    private process;
    constructor();
    get(url: string): void;
    getSync(url: string): Promise<number>;
    terminate(): void;
    on<E extends keyof VideoCountEvents>(event: E, callback: VideoCountEvents[E]): VideoCountEventEmitter;
    emit<E extends keyof VideoCountEvents>(event: E, ...args: Parameters<VideoCountEvents[E]>): boolean;
}
export default VideoCountCommand;
